global:
  scrape_interval: 2s
  evaluation_interval: 2s
  scrape_timeout: 1s

scrape_configs:
  - job_name: nim-llm
    metrics_path: /v1/metrics
    scheme: http
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/nim.json
        refresh_interval: 5s
